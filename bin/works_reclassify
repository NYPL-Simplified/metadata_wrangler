#!/usr/bin/env python
"""Classify or reclassify works."""
import os
import sys
bin_dir = os.path.split(__file__)[0]
package_dir = os.path.join(bin_dir, "..", "..")
sys.path.append(os.path.abspath(package_dir))
from metadata.core.monitor import ReclassifierMonitor
from metadata.core.scripts import RunMonitorScript
RunMonitorScript(ReclassifierMonitor).run()
